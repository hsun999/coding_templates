# coding_templates

- Follow `common/PRINCIPLES.md`.
- Use `code_dotnet` for dotnet
- Use `code_java` for java


# Project structure

- Separate classes into folders
- DAO (data access), Entities (data classes), Services (services), Common, Util, etc